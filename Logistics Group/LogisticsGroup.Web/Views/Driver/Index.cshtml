@model IEnumerable<LogisticsGroup.Domain.Entities.Driver>

@{
    ViewData["Title"] = "Керування Водіями";
}

<<<<<<< HEAD
<div class="container-fluid mt-4 mb-5">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h2 class="fw-bold" style="color: #ed3237;">
            <i class="bi bi-person-vcard me-2"></i> Список Водіїв
        </h2>
        <a asp-controller="Driver" asp-action="Create" class="btn text-white fw-bold shadow-sm px-4 py-2" style="background-color: #ed3237; border-radius: 12px;">
            <i class="bi bi-person-plus me-2"></i> Додати водія
        </a>
    </div>

    <div class="card border-0 shadow-sm rounded-4">
        <div class="card-body p-0">
            <div class="table-responsive">
                <table class="table table-hover align-middle mb-0">
                    <thead class="table-light text-muted">
                        <tr>
                            <th scope="col" class="ps-4 py-3 border-0 rounded-top-start">ПІБ водія</th>
                            <th scope="col" class="py-3 border-0">Телефон / Контакти</th>
                            <th scope="col" class="pe-4 py-3 border-0 text-end rounded-top-end">Дії</th>
                        </tr>
                    </thead>
                    <tbody>
                        @if (Model != null && Model.Any())
                        {
                            @foreach (var obj in Model)
                            {
                                <tr>
                                    <td class="ps-4 py-3 fw-bold text-dark fs-5">
                                        <div class="d-flex align-items-center">
                                            <div class="bg-light rounded-circle d-flex align-items-center justify-content-center me-3 text-secondary border" style="width: 45px; height: 45px;">
                                                <i class="bi bi-person-fill fs-4"></i>
                                            </div>
                                            @obj.FullName
                                        </div>
                                    </td>
                                    
                                    <td class="py-3 text-muted">
                                        <div class="d-flex align-items-center">
                                            <i class="bi bi-telephone-fill me-2" style="color: #ed3237;"></i>
                                            <span class="fw-semibold">@obj.Phone</span>
                                        </div>
                                    </td>
                                    
                                    <td class="pe-4 py-3 text-end">
                                        <div class="d-flex justify-content-end gap-2">
                                            <a asp-controller="Driver" asp-action="Edit" asp-route-id="@obj.Id" class="btn btn-sm btn-outline-secondary rounded-3 px-2" title="Редагувати">
                                                <i class="bi bi-pencil-square"></i>
                                            </a>
                                            <a asp-controller="Driver" asp-action="Delete" asp-route-id="@obj.Id" class="btn btn-sm btn-outline-danger rounded-3 px-2" title="Видалити">
                                                <i class="bi bi-trash3"></i>
                                            </a>
                                        </div>
                                    </td>
                                </tr>
                            }
                        }
                        else
                        {
                            <tr>
                                <td colspan="3" class="text-center py-5 text-muted">
                                    <i class="bi bi-people d-block mb-3" style="font-size: 3rem; color: #e9ecef;"></i>
                                    <h5 class="fw-bold">Штат водіїв порожній</h5>
                                    <p>Натисніть кнопку "Додати водія", щоб зареєструвати персонал.</p>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
=======
<div class="container p-3">
    <div class="row pt-4 mb-3">
        <div class="col-6">
            <h2 class="text-primary">Список Водіїв</h2>
        </div>
        <div class="col-6 text-end">
            <a asp-controller="Driver" asp-action="Create" class="btn btn-primary">
                Створити нового водія
            </a>
        </div>
    </div>

    <div class="row mb-4 p-3 border rounded bg-light align-items-center">
        <div class="col-md-7">
            <form asp-controller="Driver" asp-action="ImportFromExcel" method="post" enctype="multipart/form-data" class="d-flex">
                <input type="file" name="file" class="form-control me-2" accept=".xlsx" required />
                <button type="submit" class="btn btn-success text-nowrap">📥 Імпорт з Excel</button>
            </form>
        </div>
        <div class="col-md-5 text-end">
            <a asp-controller="Driver" asp-action="ExportToExcel" class="btn btn-outline-success">
                📤 Експорт в Excel
            </a>
        </div>
    </div>

    <table class="table table-bordered table-striped" style="width:100%">
        <thead>
            <tr>
                <th>Ім'я водія</th>
                <th>Телефон / Дані</th>
                <th>Дії</th>
            </tr>
        </thead>
        <tbody>
            @if (Model != null && Model.Any())
            {
                @foreach (var obj in Model)
                {
                            <tr>
                                <td width="40%">@obj.FullName</td>
                                <td width="30%">@obj.Phone</td>
                                <td class="text-center">
                                    <div class="w-75 btn-group" role="group">
                                        <a asp-controller="Driver" asp-action="Edit" asp-route-id="@obj.Id" class="btn btn-warning mx-2">
                                            Редагувати
                                        </a>
                                        <a asp-controller="Driver" asp-action="Delete" asp-route-id="@obj.Id" class="btn btn-danger mx-2">
                                            Видалити
                                        </a>
                                    </div>
                                </td>
                            </tr>
                }
            }
            else
            {
                    <tr>
                        <td colspan="3" class="text-center">Водіїв поки що немає.</td>
                    </tr>
            }
        </tbody>
    </table>
>>>>>>> main
</div>