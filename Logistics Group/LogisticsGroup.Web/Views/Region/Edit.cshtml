@model LogisticsGroup.Domain.Entities.Region

@{
    ViewData["Title"] = "Редагувати область";
}

<div class="container p-3">
    <div class="row pt-4">
        <h2 class="text-primary">Редагувати область</h2>
        <hr />
    </div>

    <form method="post" asp-action="Edit">
        <input type="hidden" asp-for="Id" />

        <div class="border p-3 mt-4">
            
            <div class="mb-3">
                <label asp-for="Name" class="form-label">Назва області</label>
                <input asp-for="Name" class="form-control" />
                <span asp-validation-for="Name" class="text-danger"></span>
            </div>

            <div class="mt-4">
                <button type="submit" class="btn btn-success" style="width:150px">Оновити</button>
                <a asp-controller="Region" asp-action="Index" class="btn btn-secondary" style="width:150px">
                    Скасувати
                </a>
            </div>
        </div>
    </form>
</div>

@section Scripts {
    @{
        <partial name="_ValidationScriptsPartial" />
    }
}